import { Box, Button } from '@mui/material'

export const MainMenu = () => {
  return (
    <Box>
      <Button type="button" variant="contained" sx={{mt: 3}} fullWidth>
        Список участка
      </Button>
    </Box>
  )
}